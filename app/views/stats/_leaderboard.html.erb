<% if @leaderboard.any?  %>
    <ul class="list-unstyled">
        <% @leaderboard.each_with_index { |leader, idx| %>
            
                <li class="font-weight-bold text-muted">
                    <h5>
                        <%= (idx + 1).ordinalize %> <small><%= leader %></small>
                    </h5>
                </li>
        <% } %>
    </ul>
<% else %>
    <div class="no-leaders">
        No data yet!
    </div>
<% end %>